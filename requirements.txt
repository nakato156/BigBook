# BigBook reproducible Python environment
# Tested with Python 3.12.3 on Linux. Install with:
#   python3.12 -m venv env && env/bin/python -m pip install -r requirements.txt

# Core pipeline, parquet artifacts, PCA, clustering and graph analysis
numpy==2.4.4
pandas==3.0.2
pyarrow==24.0.0
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3
matplotlib==3.10.9
networkx==3.6.1
tqdm==4.67.3

# Local API and automated tests
fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
pytest==9.0.3

# Rebuilding the optional description-embedding cache from the raw catalog.
# The model is gated: accept its Hugging Face license and set HF_TOKEN first.
torch==2.11.0
sentence-transformers==5.4.1
transformers==5.7.0
huggingface-hub==1.13.0

# Notebooks and visualizations included in the repository.
jupyter==1.1.1
ipykernel==7.2.0
seaborn==0.13.2
plotly==6.7.0
umap-learn==0.5.12
